#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(int argc, char **argv) {
    //task 1A
    long a ;
    a = 2 + 5;
    printf("%zu \n",sizeof(a));

    printf("%zu \n",sizeof(long));
    //Answer: yes, its enough for a 32bit system

    
  return 0;
}